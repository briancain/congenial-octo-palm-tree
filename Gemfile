source "https://rubygems.org"

gem "vagrant", path: "#{ENV['GOPATH']}/src/github.com/hashicorp/vagrant"
gem "pry-byebug"

group :plugins do
  gem "vagrant-vmware-desktop", path: "#{ENV['GOPATH']}/src/github.com/hashicorp/vagrant-vmware-desktop"
  gem "vagrant-share", path: "../vagrant-share"
end
